import 'package:flutter/material.dart';

/// Paleta de cores centralizada do NaRegua.
/// Baseada no protótipo Figma do Barber Booking App.
class AppColors {
  AppColors._();

  // ─── Brand Orange ────────────────────────────────────────
  static const Color primaryOrange = Color(0xFFF7941D);
  static const Color orangeLight = Color(0xFFFFAA33);
  static const Color orangeDark = Color(0xFFF77F00);

  // ─── Brand Navy ──────────────────────────────────────────
  static const Color darkNavy = Color(0xFF2D2B55);
  static const Color navyLight = Color(0xFF3D3B6E);

  // ─── Backgrounds ─────────────────────────────────────────
  static const Color white = Color(0xFFFFFFFF);
  static const Color scaffoldBg = Color(0xFFF8F8FA);
  static const Color inputFill = Color(0xFFF6F6F8);

  // ─── Borders ─────────────────────────────────────────────
  static const Color inputBorder = Color(0xFFE8E8EC);
  static const Color divider = Color(0xFFE5E7EB);

  // ─── Text ────────────────────────────────────────────────
  static const Color textDark = Color(0xFF1A1A2E);
  static const Color textGray = Color(0xFF8E8E93);
  static const Color textHint = Color(0xFFBDBDC7);

  // ─── Functional ──────────────────────────────────────────
  static const Color error = Color(0xFFFF3B30);
  static const Color success = Color(0xFF34C759);
}
